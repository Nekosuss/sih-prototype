# GET /activity-log   chronological feed: weather changes, risk updates,
#                       reroute decisions, incident reports (each with a
#                       human-readable reason) — powers the ActivityLog panel.
