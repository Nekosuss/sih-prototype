# Applies weather/hazard changes to a road segment.
#
# Primary path: explicit demo trigger via POST /weather/events (reliable and
# reproducible for a live presentation).
# Optional stretch: a background tick that randomly evolves conditions.
